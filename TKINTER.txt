Sobre o Tkinter:
import tkinter as tk

A janela principal é aberta com uma variável que serve de Domain principal pelo comando:
$Variavel$ = tk.Tk()
Todos os widgets inseriveis na janela principal têm que ter como primeira condição o nome desta variável.

Geometria:

######A ser utilizado